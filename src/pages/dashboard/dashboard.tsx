import React from 'react';

import { Card, PageHeader } from '@din-global/tools-component-library';

const Dashboard = () => {
  return (
    <Card className="page-content">
      <PageHeader title={`Welcome to Micro FE template`} />
    </Card>
  );
};

export default Dashboard;
